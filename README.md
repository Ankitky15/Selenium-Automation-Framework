# Selenium-Automation-Framework
Selenium Automation Framework is a test automation framework developed using Java, Selenium WebDriver, TestNG, and Maven. It follows the Page Object Model (POM) design pattern to automate functional test cases such as login, search, and checkout, generates Extent Reports for test execution, and uses Git for version control.
